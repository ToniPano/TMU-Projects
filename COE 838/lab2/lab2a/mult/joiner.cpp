/*#include <iostream>
#include "joiner.h"

//function is never recognized by compiler?
int i, sum;
template <int BITS>
void joiner <BITS> :: behaviour(){
	
	sum = 0;
	for(i = 0; i < BITS; i++){
		sum += (in[i].read() << i);
	}
	out.write(sum);
}*/
